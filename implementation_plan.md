# خطة تنفيذ إعادة هيكلة التطبيق — MPA

## الهدف
تحويل التطبيق من ملف HTML واحد (4000+ سطر) إلى تطبيق متعدد الصفحات (MPA) نظيف ومحترف.

## الحالة الحالية ✅
الملفات الموجودة بالفعل:
- `auth.html` — صفحة تسجيل الدخول ✅  
- `index.html` — لوحة الجدول الدراسي ✅ (مبنية مسبقاً من حساب آخر)
- `css/style.css` — الأنماط المشتركة ✅
- `js/common.js` — إدارة الحالة والـ Auth Guard ✅
- `js/particles.js` — حركة الجسيمات ✅

## الصفحات المطلوب إنشاؤها 🔧

### المجموعة الأولى — الصفحات الرئيسية
| الملف | الوصف | الأولوية |
|-------|--------|----------|
| `study.html` | جدول المذاكرة الشخصي | 1 |
| `todo.html` | قائمة المهام اليومية | 2 |
| `curriculum.html` | مدير المنهج (بدون Modals) | 3 |
| `review.html` | مراجعة الدروس المكتملة | 4 |
| `profile.html` | الملف الشخصي والإحصائيات | 5 |

### المجموعة الثانية — صفحات الذكاء الاصطناعي
| الملف | الوصف | الأولوية |
|-------|--------|----------|
| `ai/chat.html` | المحادثة مع مستشار أكاديمي | 6 |
| `ai/study.html` | مساعد المذاكرة مع رفع صور | 7 |
| `ai/exam.html` | الامتحان التكيفي (20/40/80 سؤال) | 8 |

---

## التفاصيل التقنية لكل صفحة

### `study.html`
- بطاقات إدخال مواعيد المذاكرة لكل مادة
- نفس نظام Day/Slots من تسجيل الجدول
- زر حفظ يستدعي `saveCurrentData()`
- عرض مدمج: Grid + Cards views

### `todo.html`
- عرض تاريخي (اليوم/الأمس/مخصص)
- مهام تلقائية من حصص اليوم + مذاكرة اليوم
- مهام يدوية قابلة للإضافة والحذف
- أرشفة المهام المكتملة بعد 24 ساعة
- فلترة: الكل / معلقة / مكتملة

### `curriculum.html`
- قائمة المواد بدون Modals (Full-page navigation)
- Level 1: عرض المواد → النقر يفتح قسم الوحدات
- Level 2: عرض الوحدات → النقر يفتح قسم الدروس
- إضافة/حذف وحدات ودروس
- شريط تقدم لكل مادة ووحدة

### `review.html`
- عرض المواد التي تحتوي على دروس مكتملة فقط
- Checklist للدروس مع حالة "تمت المراجعة"
- إحصائيات: كم درس راجعت / متبقي

### `profile.html`
- صورة رمزية بحرف المستخدم
- إحصائيات: عدد المواد / جلسات المذاكرة / المهام المكتملة
- حقل مفتاح Gemini API
- أزرار: تسجيل خروج / مسح البيانات

### `ai/chat.html`
- محادثة مع مستشار أكاديمي متخصص في الثانوية العامة المصرية
- System prompt مخصص بالعربية
- تاريخ المحادثة محفوظ في الجلسة

### `ai/study.html`
- اختيار المادة والوحدة والدرس أولاً
- رفع صور متعددة (base64 inline)
- System prompt مقيّد بسياق المادة المختارة
- خانات لطرح الأسئلة وشرح الصور

### `ai/exam.html`
- اختيار المادة → الوحدة → الدرس
- **Lesson Exam:** 20 سؤال MCQ (دائماً مفتوح)
- **Unit Exam:** 40 سؤال (يُفتح عند إكمال جميع دروس الوحدة + درس exam واحد على الأقل)
- **Course Exam:** 80 سؤال (يُفتح عند إكمال جميع unit exams)
- عرض الأسئلة واحداً تلو الآخر مع progress bar
- شرح فوري بعد كل إجابة

---

## ترتيب التنفيذ

1. `study.html` ← مشابه لـ screen4 في الأصل
2. `todo.html` ← مشابه لـ screen5 مع تحسينات
3. `curriculum.html` ← إعادة تصميم screen7 بدون modals
4. `review.html` ← مشابه لـ reviewView في screen7
5. `profile.html` ← مشابه لـ screen6
6. `ai/chat.html` ← من AI tab في النسخة الأصلية
7. `ai/study.html` ← من AI study tab
8. `ai/exam.html` ← من AI exam tab

---

## الملاحظات التقنية
> [!IMPORTANT]
> - جميع الصفحات تستخدم `localStorage` للبيانات (لا يوجد backend)
> - `js/common.js` يحتوي على `initAuthGuard()` الذي يحمي جميع الصفحات تلقائياً
> - صفحات الـ AI في مجلد `ai/` تستخدم `../` prefix للملفات المشتركة
> - الـ Gemini API key محفوظ في `localStorage.getItem('tt_gemini_key')`

> [!NOTE]
> - البيانات محفوظة تحت مفتاح `tt_data_{username}` في localStorage
> - المستخدم الحالي محفوظ في `localStorage.getItem('tt_current_user')`
> - دالة `callGemini()` موجودة في `common.js` مع Model Fallback تلقائي
